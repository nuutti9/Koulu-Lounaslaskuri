{
  "name": "KR-TRACK",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step needed for plain HTML'"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
